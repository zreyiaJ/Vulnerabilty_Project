@*
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@using (Html.BeginForm("Login", "Account", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        <label>Username</label>
        @Html.TextBox("userName", null, new { @class = "form-control", placeholder = "Enter your username" })
    </div>

    <div class="form-group">
        <label>Password</label>
        @Html.Password("password", null, new { @class = "form-control", placeholder = "Enter your password" })
    </div>

    <button type="submit" class="btn btn-primary">Login</button>

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
}*@
<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <!-- Add styles specific to login page here -->
</head>
<body>
    <div class="login-container">
        @RenderBody() <!-- This is where the content of Login.cshtml will be injected -->
    </div>
</body>
</html>